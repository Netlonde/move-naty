export interface FormRequiredFields {
  initialKm: string;
  departureStart: string;
  checklist: string;
  reason: string;
  observation: string;
  driverId: string;
  vehicleId: string;
  clientId: string;
}
